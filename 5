#include "binary_trees.h"

/**
  * binary_tree_is_root - a function that checks if a node is a leaf
  * @node: node to test if leaf
  *
  * Return: 1 or 0
  */

int binary_tree_is_root(const binary_tree_t *node)
{
	if (!node)
		return (0);
	node->parent ? return (0) : return (1);
}
